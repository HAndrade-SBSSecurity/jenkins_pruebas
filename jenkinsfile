pipeline {
    agent any

    stages {
        stage('Clone Repo "do"') {
            steps {
                script {
                    while (true) {
                        sleep time: 20, unit: 'SECONDS' // Espera 3 minutos
                        sh 'python3 do.py' // o viene un archivo python?
                        
                    }
                }
            }
        }

        stage('Clone Repo "to do"') {
            steps {
                script {
                    while (true) {
                        sleep time: 51, unit: 'SECONDS' // Espera 5 minutos
                        sh 'python3 to_do.py' // o viene un archivo python?
                        
                    }
                }
            }
        }
    }
}